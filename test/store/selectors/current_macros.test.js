import { Map } from 'immutable';
import chai, { expect } from 'chai';
import chaiImmutable from 'chai-immutable';
import moment from 'moment';

chai.use(chaiImmutable);


describe('currrent macros selector', () => {
  it('description', () => {

  });
});
