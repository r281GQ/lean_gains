import React from 'react';
import PropTypes from 'prop-types';
import ImmutablePropTypes from 'react-immutable-proptypes';
import { Dialog, FlatButton } from 'material-ui';

const Error = () =>
// const Error = ({ errorMessages, isErrorModalOpen, closeErrorModal }) =>
<div></div>
  // <Dialog
  //   open={isErrorModalOpen}
  //   action={[<FlatButton key="ok" label="ok" onClick={closeErrorModal} />]}
  // >
  //   {errorMessages}
  // </Dialog>;

// Error.propTypes = {
//   errorMessages: ImmutablePropTypes.listOf(PropTypes.string),
//   isErrorModalOpen: PropTypes.bool.isRequired,
//   closeErrorModal: PropTypes.func.isRequired
// };

export default Error;
